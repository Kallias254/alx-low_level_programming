#include "main.h"

/**
 * _strlen - return the length of a string.
 *@s: the string array passed
 * Return: Always 0 successful.
 *
 */
int _strlen(char *s)
{
	while (*s != '\0')
	{
		*s++;
	}

return (s);
}
