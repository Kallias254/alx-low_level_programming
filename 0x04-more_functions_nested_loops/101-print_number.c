#include "main.h"

/**
 * print_number - print an integer
 * @n: integer to be printed
 *
 */
void print_number(int n)
{
	if (n > 0)
	{
		over_10();

	} else if (n == 0)
	{

	} else if (n < 0)
	{

	}
	if (last >= 0)
		_putchar();


}

int over_10(void)
{
	for (i = 0 ; i <= 4 ; i++)
	{
		if (last >= 0)

		last_0 = n % 10;
		last_1 = (n / 10) % 10;
		last_2 = (n / 100) % 10;
		last_3 = (n / 1000) % 10;
	}

}
